# Copy to .env and fill in. Never commit the real .env file.

# Ollama endpoint on the Windows inference host
OLLAMA_HOST=http://<windows-box-ip-or-hostname>:11434

# Web search tool (once Phase 3 is built) — pick one
# SEARXNG_URL=
# BRAVE_SEARCH_API_KEY=
